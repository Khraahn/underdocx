- Features
  - PartialExtractor workaround ausbauen
  - FullPathExtractor workaround ausbauen
  - DollarImagePlaceholdersProvider workaround ausbauen
  - ImagePlaceholdersProvider workarund
  - CachedEnumerator, dessen Klone seinen eigenen Speichert erweitern, ohne den Enumerationszustand zu verändern.
    => Für Platzhalterenumerator nutzen, so dass LookAhead keinen Mehrfachbesuch verursacht
  - später ggf. docx unterstützen
- Bekannte Probleme:
  - Prüfen ob Vectorgrafiken als Bilder verwendet werden können
- Optimierungsmöglichkeiten:
  - getPlaceholderData sollte bei allem Handlern gecached werden (StringOutputModule)
  - Cache für Paar-Knoten (For-EndFor, If-EndIf) // reduziert das Scannen zukünftiger Knoten)
